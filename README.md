# F1 Telemetry — Python / Spring / React

This repository contains a small telemetry pipeline:
- `python-service` — fetches F1 lap times (fastf1) and writes to MongoDB
- `spring-backend` — Spring Boot API exposing lap data from MongoDB
- `react-frontend` — Vite React dashboard showing lap charts

